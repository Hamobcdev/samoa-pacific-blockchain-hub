# @samoa-dpi/verify

Public document verification portal — any user can verify a government-issued credential hash against the NDIDS Registry without revealing PII.

**Target subdomain:** `samoa-dpi-verify.vercel.app`
(Production target: verify.dpi.gov.ws — pending MCIT authorisation)
