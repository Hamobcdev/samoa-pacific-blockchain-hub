{
  "name": "samoa-pacific-blockchain-hub-frontend",
  "version": "2.0.0",
  "description": "Samoa Government Interoperability PoC Dashboard — UNICEF Venture Fund — Live ethers.js contract reads",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "gh-pages": "^6.0.0",
    "vite": "^5.0.0"
  }
}

