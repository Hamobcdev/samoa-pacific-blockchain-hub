# @samoa-dpi/auth

Shared authentication utilities: DID-based identity verification, MetaMask wallet connection helpers, demo-mode wallet, and role-based access guards.

Used by `apps/ministry`, `apps/admin`, and `apps/donor`.
